
  # Internal Service Desk UI

  This is a code bundle for Internal Service Desk UI. The original project is available at https://www.figma.com/design/fEt97K9S5IJuM5ueBlZ9RQ/Internal-Service-Desk-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  